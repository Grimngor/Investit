# Local split-server development values. Leave these unset in Docker builds so
# the production frontend uses same-origin /api and /ws through the web proxy.
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws
